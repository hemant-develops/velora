// Deprecated: the old hardcoded demo conversations (Driver Support, VELORA
// Support, etc.) have been removed. Messaging is now fully live — a
// conversation is created the moment a renter sends a real message to a
// car's owner from Car Details, and only then does it appear to either
// side. See src/context/MessagesContext.tsx.
export {};
